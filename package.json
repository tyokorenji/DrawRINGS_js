{
  "name": "drawRINGS-js",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "rm -rf dist/*.js && webpack --config webpack-release.config.js --progress",
    "build": "rm -rf dist/*.js && webpack --config webpack.config.js --progress",
    "watch": "rm -rf dist/*.js && webpack --config webpack.config.js --progress --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-loader": "^6.2.7",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "exports-loader": "^0.6.3",
    "imports-loader": "^0.6.5",
    "json-loader": "^0.5.4",
    "webpack": "^1.13.3"
  }
}
